const top = require("./top");
const ready = require("./ready");
const game = require("./game");
const winner = require("./winner");
const gameEnd = require("./gameEnd");

module.exports = { top, ready, game, winner, gameEnd };
