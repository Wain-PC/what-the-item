import { SET_PLAYER_READY } from "../constants/actions";

const setPlayerReady = () => ({
  type: SET_PLAYER_READY
});

// eslint-disable-next-line import/prefer-default-export
export { setPlayerReady };
